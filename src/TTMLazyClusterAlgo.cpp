#include "TTMLazyClusterAlgo.h"

#include "TopTaggerResults.h"

void TTMLazyClusterAlgo::run(TopTaggerResults& ttResults)
{
    const std::vector<const Constituent>& constituents = ttResults.getConstituents();
}
